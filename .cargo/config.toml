[env]
# Build against the committed .sqlx/ cache, not a live Postgres. A query edit
# then fails the next local build instead of surfacing on a PR.
#
# Not forced, so an explicit env var still wins:
#   SQLX_OFFLINE=false cargo sqlx prepare --workspace -- --all-targets
SQLX_OFFLINE = "true"
