# Rust workspace build output (shared by apps/server, shared, engine — each
# also has its own /target ignore for standalone builds)
/target

# server local env
apps/server/.env

# Agent / local tooling
.claude/

# OS cruft
.DS_Store

# SDD scratch workspace (ledger, briefs, review packages)
.superpowers/

# A small sample of maimai songs for testing.
songs/

# Real charts used as a local parser smoke test. The committed fixtures beside
# this directory are hand-written; upstream chart text stays out of the repo.
# The corpus test sweeps these for panics when present and ignores them when not.
engine/tests/fixtures/local/
