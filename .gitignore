# Rust workspace build output (shared by apps/server, apps/host/src-tauri,
# shared, engine — each also has its own /target ignore for standalone builds)
/target

# server local env
apps/server/.env

# Agent / local tooling
.claude/

# OS cruft
.DS_Store
